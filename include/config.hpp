#pragma once

#include <SFML/Graphics.hpp>

namespace config {
    inline constexpr int initial_grid_size = 10;
    inline constexpr sf::Vector2u window_size = {800, 800};
    inline constexpr int layer_amount = 5;
}